BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[tyres] DROP CONSTRAINT [tyres_installedUnitId_positionTyre_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
