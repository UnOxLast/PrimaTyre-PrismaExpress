BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[InspectionTyre] DROP CONSTRAINT [InspectionTyre_actionTyreId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
