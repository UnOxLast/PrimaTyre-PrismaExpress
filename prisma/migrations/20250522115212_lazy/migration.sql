BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[InspectionTyre] ADD CONSTRAINT [InspectionTyre_isReady_df] DEFAULT 0 FOR [isReady];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
