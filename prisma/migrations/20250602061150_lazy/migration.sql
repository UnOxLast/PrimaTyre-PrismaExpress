BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Unit] ADD CONSTRAINT [Unit_hmUnit_df] DEFAULT 0 FOR [hmUnit], CONSTRAINT [Unit_kmUnit_df] DEFAULT 0 FOR [kmUnit];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
