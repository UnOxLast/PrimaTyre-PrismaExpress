BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[ActivityTyre] ALTER COLUMN [dateTimeWork] DATETIME2 NULL;

-- AlterTable
ALTER TABLE [dbo].[tyres] ADD [hmTyre] INT,
[kmTyre] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
